# Wordpress-brute-forcer
 🔐 Multi-threaded WordPress login brute forcer. Pure HTTP, no browser. Threads, proxy support, WAF evasion delays, auto WP verification, zero false positives. For authorized pentesting only.
